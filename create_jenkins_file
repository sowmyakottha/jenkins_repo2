node {
  stage('Build') {
      // Run Build commands
      sh echo '"Building the job......"'
  }
  stage('Test') {
      // Run test commands
      sh echo '"Test the job...."'
  }
  stage('Deploy') {
      // Deploy command
      sh echo '"Deploy the project...."'
  }
}     
  
      
